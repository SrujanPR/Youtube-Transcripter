# ── YouTube Transcript Generator — Environment Variables ──────
# All variables are optional. The app works out of the box using
# free Invidious/Piped public API instances.

# Optional: Residential proxy for the "direct" strategy
# (only needed if Invidious + Piped are both down)
# PROXY_URL=http://user:pass@host:port

# Optional: Override default Invidious instances (comma-separated)
# INVIDIOUS_INSTANCES=https://inv.nadeko.net,https://yewtu.be,https://vid.puffyan.us

# Optional: Override default Piped instances (comma-separated)
# PIPED_INSTANCES=https://pipedapi.kavin.rocks
